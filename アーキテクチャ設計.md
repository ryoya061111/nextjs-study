# React/Next.js学習プロジェクト - アーキテクチャ設計

## プロジェクト概要

学習用フロントエンドWebアプリケーション。Next.js + React + TypeScript（SSR）を使用した、TODO CRUD機能を持つアプリケーション。

### プロジェクト要件

| 項目 | 内容 |
|------|------|
| フロントエンド | Next.js、React、TypeScript、SSR |
| 状態管理 | Context API + Hooks（※TanStack Queryは後対応） |
| フォーム管理 | React Hook Form + Zod |
| API連携 | 純粋関数モック実装（※MSWは後対応） |
| 主要機能 | TODO CRUD、ログイン管理（モック） |
| 対象学習者 | React初心者〜中級者 |

---

## 機能追加の進め方（開発着手順序）

新しい機能を追加するときは必ず以下の順番で作る。
上から作ることで「型が合わない」「データがない」などの手戻りを防げる。

```
① types/          型定義・バリデーションスキーマ
      ↓
② services/       データ層（モック・Repository・Service）
      ↓
③ app/api/        API Routes（モックバックエンド）
      ↓
④ hooks/          カスタムフック（CRUDロジック）
      ↓
⑤ components/     コンポーネント（小さい単位から）
      ↓
⑥ app/            ページ（Server Component → Client Component を組み立てる）
```

### 各ステップの詳細

#### ① 型定義（types/）から始める理由

> **役割：データの形（構造）を定義する。すべての層がここで決めた型に従うため、設計全体の土台になる。**

型を先に定めることで、その機能の「データの形」が確定する。
後続のすべての実装がこの型に従うため、**設計の矛盾を最も早く発見できる**。

```
作成ファイル:
  types/xxx/shared/xxx.types.ts   ← エンティティ型（全ページで使う共通型）
  types/xxx/shared/xxx.schema.ts  ← Zodバリデーションスキーマ（フォームの入力値の形）
  types/xxx/list/xxx.types.ts     ← 一覧ページ専用の型（CreateXxxRequest など）
  types/xxx/edit/xxx.types.ts     ← 編集ページ専用の型（UpdateXxxRequest など）
```

#### ② データ層（services/）

> **役割：データの取得・操作ロジックを担う。どこからデータを得てどう加工するかを一元管理する場所。**

型が決まったらデータの準備をする。
APIより先にデータ層を固めることで、API・コンポーネント両方から参照できる。

```
作成ファイル:
  services/mockData.ts        ← モックデータ（インメモリ配列。バックエンドの代わり）
  services/xxxRepository.ts   ← Server Component 用（データソースに直接アクセス）
  services/xxxService.ts      ← Client Component 用（fetch で API Route を呼び出すラッパー）
```

#### ③ API Routes（app/api/）

> **役割：クライアントからHTTPで呼び出せるエンドポイントを提供する。将来バックエンドに差し替えることを想定したモック窓口。**

データ層ができたら、クライアントからHTTPで叩けるエンドポイントを作る。
この時点でブラウザの開発者ツールでAPIの動作確認ができるようになる。

```
作成ファイル:
  app/api/xxx/route.ts        ← GET（一覧）/ POST（作成）
  app/api/xxx/[id]/route.ts   ← GET（1件）/ PUT（更新）/ DELETE（削除）
```

#### ④ カスタムフック（hooks/）

> **役割：APIの呼び出しと状態管理をコンポーネントから切り離す。UIとロジックの責務を分離する層。**

API が決まったら、それを呼び出すロジックをフックにまとめる。
コンポーネントは「どう見せるか」だけに集中できるようになる。

```
作成ファイル:
  hooks/useXxx.ts  ← useState / CRUD操作 / ローディング・エラー管理
```

#### ⑤ コンポーネント（components/）

> **役割：データの取得・表示・インタラクションをすべて components/ 内で完結させる。page.tsx はURLの窓口に徹する。**

フックができたら UI を組み立てる。**小さい単位から大きい単位へ**作る。

```
作成順:
  ui/              汎用パーツ（Button, Input など）
      ↓
  XxxItem          1件分の表示（最小単位）
      ↓
  XxxForm          入力フォーム（React Hook Form + Zod）
      ↓
  XxxList          一覧表示（Item + Form を組み合わせる）← Client Component
      ↓
  XxxFetcher       データ取得 → XxxList に渡す         ← Server Component
```

`XxxFetcher`（Server Component）がデータを取得し、`XxxList`（Client Component）に props として渡す。
この2層構造にすることで「データ取得」と「表示・インタラクション」が明確に分離される。

#### ⑥ ページ（app/）

> **役割：URLの窓口。params を受け取って Fetcher を呼ぶだけ。データ取得・表示ロジックは持たない。**

最後にページを作る。ページは「URLを受け取って components/ に渡すだけ」の薄い層。

```
// ✅ このプロジェクトの書き方（page.tsx は薄く）
export default async function EditTodoPage({ params }) {
  const { id } = await params        // params の受け取りだけここでやる
  return <TodoEditFetcher id={id} /> // あとは Fetcher に任せる
}

// ❌ やらない書き方（page.tsx でデータ取得まで）
export default async function EditTodoPage({ params }) {
  const { id } = await params
  const todo = await todoRepository.getById(id)  // ← これを page.tsx に書かない
  return <TodoEditForm todo={todo} />
}
```

### TODO機能での実例

```
① types/todo/shared/todo.types.ts   Todo型（全ページ共通）
   types/todo/shared/todo.schema.ts  todoFormSchema・TodoFormValues（フォーム共通）
   types/todo/list/todo.types.ts     CreateTodoRequest（作成フロー専用）
   types/todo/edit/todo.types.ts     UpdateTodoRequest（編集フロー専用）
      ↓
② services/todo/shared/mockData.ts        初期モックデータ配列
   services/todo/shared/todoRepository.ts Server Component用（getAll/getById）
   services/todo/list/todoService.ts      一覧ページ用（create/delete）
   services/todo/edit/todoService.ts      編集ページ用（update）
      ↓
③ app/api/todos/route.ts     GET・POST
   app/api/todos/[id]/route.ts  GET・PUT・DELETE
      ↓
④ hooks/todo/list/useTodos.ts   createTodo/toggleTodo/deleteTodo + useState
      ↓
⑤ components/todos/shared/TodoForm.tsx      フォーム（React Hook Form + Zod）※作成・編集で共用
   components/todos/list/TodoItem.tsx      1件表示・削除・完了切替（Client Component）
   components/todos/list/TodoList.tsx      一覧 + 新規作成フォーム（Client Component）
   components/todos/list/TodoListFetcher.tsx  データ取得 → TodoList に渡す（Server Component）
   components/todos/edit/TodoEditForm.tsx  編集フォーム（Client Component）
   components/todos/edit/TodoEditFetcher.tsx  データ取得 → TodoEditForm に渡す（Server Component）
      ↓
⑥ app/(dashboard)/todos/page.tsx             URLの窓口。<TodoListFetcher /> を呼ぶだけ
   app/(dashboard)/todos/[id]/edit/page.tsx  params から id を取り出して <TodoEditFetcher id={id} /> を呼ぶだけ
```

---

## システム全体構成図

```
                      リクエスト                    レスポンス
┌──────────────────────────────────────────────────────────────┐
│                        ブラウザ                               │
│                 （HTML + CSS + JavaScript）                   │
└───────────────┬──────────────────────────┬───────────────────┘
                │ HTTPリクエスト            △ HTML/JSON返却
                ▼                          │
┌──────────────────────────────────────────────────────────────┐
│                    Node.js ランタイム                         │  ← サーバー実行環境
│  ┌────────────────────────────────────────────────────────┐  │
│  │              Next.js SSRアプリケーション                │  │
│  │             (Server + Client Components)               │  │
│  │                                                        │  │
│  │  ・Server Components（SSRレンダリング）                 │  │
│  │  ・API Routes（モックバックエンド）                     │  │
│  └────────────────┬───────────────────────┬───────────────┘  │
└───────────────────│───────────────────────│──────────────────┘
                    │ データ要求             △ データ返却
                    ▼                       │
       ┌────────────────────────────────────────────┐
       │          React Components + Hooks          │
       │    ・Pages（ページコンポーネント）          │
       │    ・Components（UIコンポーネント）        │
       │    ・Custom Hooks（ロジック）              │
       └────────────┬───────────────────────┬───────┘
                    │ API呼び出し            △ レスポンス
                    ▼                       │
       ┌────────────────────────────────────────────┐
       │             API Layer（モック）             │
       │    ・Services（API呼び出し層）              │
       │    ・Mock Data（モックデータ）              │
       └────────────┬───────────────────────┬───────┘
                    │ 状態更新              △ 現在の状態
                    ▼                       │
       ┌────────────────────────────────────────────┐
       │            State Management                │
       │    ・Context（グローバル状態）              │
       │    ・Hooks（ローカル状態）                 │
       └────────────────────────────────────────────┘
```

---

## プロジェクトディレクトリ構造

```
reactnextjs/
│
├── app/                              # Next.js 13+ App Router
│   ├── layout.tsx                    # ルートレイアウト（全ページ共通）
│   ├── page.tsx                      # ホームページ
│   ├── loading.tsx                   # ローディング画面
│   ├── error.tsx                     # エラーハンドリング
│   │
│   ├── (auth)/                       # Route Group：ログイン関連
│   │   ├── layout.tsx
│   │   ├── login/
│   │   │   └── page.tsx              # ログインページ
│   │   └── register/
│   │       └── page.tsx              # ユーザー登録ページ
│   │
│   ├── (dashboard)/                  # Route Group：ダッシュボード（認証後）
│   │   ├── layout.tsx                # ダッシュボードレイアウト
│   │   ├── page.tsx                  # ダッシュボードホーム
│   │   └── todos/
│   │       ├── page.tsx              # TODO一覧（GET）
│   │       ├── [id]/
│   │       │   └── page.tsx          # TODO詳細編集（PUT/DELETE）
│   │       └── create/
│   │           └── page.tsx          # TODO作成（POST）
│   │
│   └── api/                          # API Routes（バックエンド代わり）
│       ├── auth/
│       │   ├── [...nextauth]/
│       │   │   └── route.ts          # Auth.js ハンドラ（GET/POST）
│       │   └── login/route.ts        # POST /api/auth/login（モック）
│       ├── todos/
│       │   ├── route.ts              # GET /api/todos, POST /api/todos
│       │   └── [id]/route.ts         # GET/PUT/DELETE /api/todos/[id]
│       └── health/
│           └── route.ts              # ヘルスチェック
│
├── components/                       # 再利用可能なReactコンポーネント
│   ├── common/                       # 共通コンポーネント
│   │   ├── Header.tsx                # ヘッダー
│   │   ├── Sidebar.tsx               # サイドバー
│   │   ├── Footer.tsx                # フッター
│   │   ├── Navigation.tsx            # ナビゲーション
│   │   └── ErrorBoundary.tsx         # コンポーネントレベルのエラー境界
│   │
│   ├── auth/                         # 認証関連コンポーネント
│   │   ├── LoginForm.tsx             # ログインフォーム
│   │   ├── RegisterForm.tsx          # ユーザー登録フォーム
│   │   └── ProtectedRoute.tsx        # 認証保護コンポーネント
│   │
│   ├── todos/                        # TODO関連コンポーネント（ページ単位でサブフォルダ管理）
│   │   ├── list/                     # /todos ページ専用
│   │   │   ├── TodoListFetcher.tsx   # データ取得 → TodoList に渡す（Server Component）
│   │   │   ├── TodoList.tsx          # 一覧表示・操作（Client Component）
│   │   │   └── TodoItem.tsx          # TODOアイテム（1行）
│   │   ├── edit/                     # /todos/[id]/edit ページ専用
│   │   │   ├── TodoEditFetcher.tsx   # データ取得 → TodoEditForm に渡す（Server Component）
│   │   │   └── TodoEditForm.tsx      # 編集フォーム（Client Component）
│   │   └── shared/                   # 複数ページから使う共通コンポーネント
│   │       └── TodoForm.tsx          # 作成・編集フォーム（list / edit 両方から使用）
│   │
│   └── ui/                           # 全機能共通の汎用UIパーツ
│       ├── Button.tsx                # variant: primary/secondary/danger/ghost、size: sm/md
│       ├── Input.tsx                 # label + input + エラー表示（RHF forwardRef 対応）
│       ├── Textarea.tsx              # label + textarea + エラー表示（RHF forwardRef 対応）
│       ├── Checkbox.tsx              # チェックボックス（RHF forwardRef 対応）
│       ├── Card.tsx                  # ボーダー付きコンテナ
│       ├── LoadingSpinner.tsx        # 処理中インジケーター
│       └── FormErrorMessage.tsx     # API エラーの赤ボックス表示
│
├── hooks/                            # カスタムフック（機能・ページ単位でサブフォルダ管理）
│   ├── todo/
│   │   ├── list/
│   │   │   └── useTodos.ts           # 一覧ページ専用（作成・切替・削除 + 状態管理）
│   │   └── edit/                     # 編集ページ専用（現在は空）
│   └── auth/
│       └── login/
│           └── useAuth.ts            # 認証ロジック（将来追加）
│
├── context/                          # React Context（グローバル状態管理）
│   ├── AuthContext.tsx               # ユーザー認証状態
│   ├── TodoContext.tsx               # TODO状態
│   └── providers.tsx                 # Context Providers統合
│
├── services/                         # データアクセス層（機能・ページ単位でサブフォルダ管理）
│   ├── todo/
│   │   ├── shared/
│   │   │   ├── mockData.ts           # モックデータ（API Route・Repository 両方から参照）
│   │   │   └── todoRepository.ts     # Server Component 用（getAll/getById）
│   │   ├── list/
│   │   │   └── todoService.ts        # 一覧ページ専用（create・delete）
│   │   └── edit/
│   │       └── todoService.ts        # 編集ページ専用（update）※完了トグルも使用
│   └── auth/
│       └── authService.ts            # 認証 API（将来追加）
│
├── types/                            # TypeScript型定義（機能・ページ単位でサブフォルダ管理）
│   ├── todo/
│   │   ├── shared/                   # 複数ページで使う共通型
│   │   │   ├── todo.types.ts         # Todo エンティティ型（全ページで参照）
│   │   │   └── todo.schema.ts        # Zod スキーマ・TodoFormValues（shared/TodoForm が使用）
│   │   ├── list/                     # /todos ページ専用
│   │   │   └── todo.types.ts         # CreateTodoRequest
│   │   └── edit/                     # /todos/[id]/edit ページ専用
│   │       └── todo.types.ts         # UpdateTodoRequest
│   ├── auth/
│   │   ├── shared/
│   │   │   └── auth.types.ts         # User エンティティ型
│   │   └── login/
│   │       └── auth.schema.ts        # ログインフォームの Zod スキーマ
│   └── common/
│       └── api.types.ts              # ApiResponse など横断的な共通型
│
├── utils/                            # ユーティリティ関数
│   ├── validators.ts                 # バリデーション関数
│   ├── formatters.ts                 # データフォーマット関数
│   ├── constants.ts                  # アプリケーション定数
│   └── helpers.ts                    # その他ヘルパー関数
│
├── styles/                           # CSSスタイル
│   ├── globals.css                   # グローバルスタイル
│   ├── variables.css                 # CSS変数（色、フォントなど）
│   └── components.css                # コンポーネント個別スタイル
│
├── public/                           # 静的アセット
│   ├── images/
│   ├── icons/
│   └── favicon.ico
│
├── __tests__/                        # 単体・統合テスト（Vitest + Testing Library）
│   ├── components/                   # コンポーネントテスト
│   ├── hooks/                        # カスタムフックテスト
│   └── services/                     # サービス層テスト
│
├── e2e/                              # E2Eテスト（Playwright）
│   ├── auth.spec.ts                  # 認証フロー
│   └── todos.spec.ts                 # TODO CRUD操作
│
├── .env.local                        # 環境変数（ローカル開発用）
├── .env.example                      # 環境変数テンプレート
├── .husky/                           # Huskyフック設定
│   └── pre-commit                    # コミット前にlint-stagedを実行
├── .gitignore                        # Git除外ファイル
├── .eslintrc.json                    # ESLint設定
├── .prettierrc                       # Prettier設定
├── .lintstagedrc.js                  # lint-staged対象ファイル設定
├── vitest.config.ts                  # Vitestテスト設定
├── playwright.config.ts              # PlaywrightE2E設定
├── package.json                      # NPM依存パッケージ
├── package-lock.json
├── tsconfig.json                     # TypeScript設定
├── next.config.js                    # Next.js設定
├── postcss.config.js                 # PostCSS設定
├── tailwind.config.js                # Tailwind CSS設定（オプション）
│
├── README.md                         # プロジェクト説明
├── アーキテクチャ設計.md             # このファイル
└── 概要                              # プロジェクト要件
```

---

## Next.js App Router お約束一覧

### 1. 予約済みファイル名（Special Files）

App Router では特定のファイル名に特別な意味が割り当てられている。

| ファイル名 | 役割 | このプロジェクトでの使用箇所 |
|-----------|------|--------------------------|
| `page.tsx` | そのURLのページ本体。このファイルがないとURLとして認識されない | `todos/page.tsx` など |
| `layout.tsx` | 複数ページで共通するUIのラッパー（ナビゲーション等）。子ページが切り替わっても再レンダリングされない | `app/layout.tsx` |
| `loading.tsx` | ページのデータ取得中に表示するUI（Suspenseの自動ラッパー） | 未実装（後対応） |
| `error.tsx` | ページでエラーが発生したときに表示するUI。`'use client'` 必須 | 未実装（後対応） |
| `not-found.tsx` | `notFound()` を呼んだとき、またはURLが存在しないときに表示するUI | 未実装（後対応） |
| `route.ts` | API エンドポイント定義。`page.tsx` と同じフォルダには置けない | `app/api/todos/route.ts` など |
| `template.tsx` | `layout.tsx` と似ているが、ページ遷移のたびに再マウントされる（アニメーション等に使用） | 未使用 |

---

### 2. 予約済みフォルダ名（Special Folders）

| フォルダ名 | 役割 | 例 |
|-----------|------|-----|
| `[param]` | **動的セグメント**。角括弧内の名前がURLパラメータ名になる | `[id]` → `/todos/123` で `params.id = "123"` |
| `[...param]` | **キャッチオール**。後続のパス全部をまとめて配列で受け取る | `[...slug]` → `/a/b/c` で `params.slug = ["a","b","c"]` |
| `[[...param]]` | **オプショナルキャッチオール**。パラメータがなくてもマッチする | `[[...slug]]` → `/` でもマッチ |
| `(group)` | **ルートグループ**。URLには影響せずレイアウトのグループ分けだけに使う | `(dashboard)` → URLは `/todos`（dashboardは非表示） |
| `_folder` | **プライベートフォルダ**。ルーティングの対象から除外される | `_components/` |
| `@folder` | **名前付きスロット**。並列ルーティング（Parallel Routes）で使用 | 未使用 |

```
app/
├── (dashboard)/          ← URLに出ない。ログイン後のレイアウトグループ
│   └── todos/
│       ├── page.tsx      ← /todos
│       └── [id]/         ← /todos/:id
│           └── edit/
│               └── page.tsx  ← /todos/:id/edit
└── api/
    └── todos/
        ├── route.ts      ← GET /api/todos, POST /api/todos
        └── [id]/
            └── route.ts  ← GET/PUT/DELETE /api/todos/:id
```

---

### 3. layout.tsx の入れ子ルール

`page.tsx` にアクセスがあると、Next.js は**同じフォルダ以上にある `layout.tsx` を外側から順番に自動で被せる**。
自分で import したり呼び出したりする必要はなく、**ファイルを置くだけで適用される**のがお約束。

#### 読み込み順

```
app/layout.tsx                    ← 1番目（全ページ共通）
  └── app/(dashboard)/layout.tsx  ← 2番目（(dashboard) 配下のみ）
        └── app/(dashboard)/todos/page.tsx  ← 最後にページ本体
```

#### 仕組み

`layout.tsx` は `{children}` で子ページを受け取って包む構造になっている。

```tsx
// (dashboard)/layout.tsx
export default function DashboardLayout({ children }) {
  return (
    <div>
      <Header />
      <main>{children}</main>  {/* ← ここに todos/page.tsx の中身が入る */}
    </div>
  )
}
```

#### このプロジェクトのレイアウト構成

```
app/layout.tsx              ← 全ページ共通（html・body・フォント設定）
├── app/auth/layout.tsx     ← /auth/* のみ（中央寄せ背景。Header なし）
│   └── /auth/login
└── app/(dashboard)/layout.tsx  ← /todos/* のみ（Header あり）
    ├── /todos
    └── /todos/[id]/edit
```

`(dashboard)` の中に新しいページを追加するだけで、自動的に Header が付く。
逆に `auth/` 配下に追加したページには Header は付かない。

---

### 4. Server Component と Client Component

| | Server Component | Client Component |
|--|-----------------|-----------------|
| 宣言方法 | ファイル先頭に何も書かない（**デフォルト**） | ファイル先頭に `'use client'` |
| 実行場所 | **サーバーのみ**（ブラウザには届かない） | サーバーでHTMLを生成 + ブラウザでHydration |
| できること | DBアクセス・秘密情報の使用・async/await | useState・useEffect・イベントハンドラ |
| できないこと | useState・useEffect・ブラウザAPI | サーバーサイドのみの処理 |
| データ取得 | `todoRepository`（直接アクセス） | `todoService`（fetch経由） |

```typescript
// Server Component（デフォルト）
// ✅ async/await が使える
// ✅ DBやファイルに直接アクセスできる
// ❌ useState / useEffect は使えない
export default async function Page() {
  const data = await db.query(...)
  return <div>{data}</div>
}

// Client Component
'use client'
// ✅ useState / useEffect が使える
// ✅ クリックなどのイベントが扱える
// ❌ サーバー専用リソースにアクセスできない
export default function Page() {
  const [count, setCount] = useState(0)
  return <button onClick={() => setCount(c => c + 1)}>{count}</button>
}
```

---

### 5. API Route の HTTP メソッド対応

`route.ts` では **関数名がそのままHTTPメソッド名**になる。

```typescript
// app/api/todos/route.ts
export async function GET(request: Request) {}     // GET    /api/todos
export async function POST(request: Request) {}    // POST   /api/todos
export async function PUT(request: Request) {}     // PUT    /api/todos
export async function DELETE(request: Request) {}  // DELETE /api/todos
export async function PATCH(request: Request) {}   // PATCH  /api/todos
```

---

### 6. 動的ルートのパラメータ取得（Next.js 15 以降）

Next.js 15 から `params` が **Promise 型**に変更された。必ず `await` が必要。

```typescript
// ❌ Next.js 14 以前の書き方（15では型エラー）
export default function Page({ params }: { params: { id: string } }) {
  const { id } = params
}

// ✅ Next.js 15 以降の書き方
export default async function Page({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params  // ← await が必要
}
```

---

### 7. クエリパラメータの取得

URL の `?key=value` 形式のパラメータ。`params`（パスパラメータ）とは別物。

| | params | searchParams |
|--|--------|-------------|
| URL例 | `/todos/123` | `/todos?page=1&sort=asc` |
| 定義場所 | フォルダ名 `[id]` で決まる | 定義不要・任意で付与 |
| 型 | `Promise<{ id: string }>` | `Promise<{ [key]: string \| string[] \| undefined }>` |

```typescript
// Server Component での受け取り方
// searchParams も Next.js 15 から Promise 型 → await 必須
type Props = {
  params: Promise<{ id: string }>
  searchParams: Promise<{ page?: string; sort?: string }>
}

export default async function Page({ params, searchParams }: Props) {
  const { id } = await params
  const { page = '1', sort = 'asc' } = await searchParams
}
```

```typescript
// Client Component での受け取り方
'use client'
import { useSearchParams } from 'next/navigation'

const searchParams = useSearchParams()
const page = searchParams.get('page')   // 存在しない場合は null
const sort = searchParams.get('sort')
```

```typescript
// パラメータ付きで遷移する場合
router.push('/todos?page=2&sort=desc')
```

---

### 8. proxy.ts（認証ガード）

Next.js 16 から `middleware.ts` は `proxy.ts` に改名された（旧名は非推奨）。
すべてのリクエストの**前**に実行されるフックで、主に認証チェックやリダイレクトに使う。

#### 実行タイミング

`config.matcher` に指定したパスへのリクエストがあるたびに毎回実行される。
Cookie の JWT を読むだけなので処理は軽く、DB アクセスは発生しない。

```typescript
// src/proxy.ts
import { auth } from '@/auth'
import { NextResponse } from 'next/server'

export default auth((req) => {
  if (!req.auth) {
    return NextResponse.redirect(new URL('/auth/login', req.url))
  }
})

export const config = {
  matcher: ['/todos/:path*'],
}
```

#### matcher の書き方：2つのアプローチ

**① 許可リスト（保護するルートを明示）**
新しいルートを追加するたびに matcher も更新が必要。ルートが少ないうちはこちらが分かりやすい。

```typescript
export const config = {
  matcher: [
    '/todos/:path*',
    '/dashboard/:path*',  // 新しい保護ルートはここに追加
    '/profile/:path*',
  ],
}
```

**② 除外リスト（公開ページだけ除外・推奨）**
新しいページを追加しても何もしなくていい。認証し忘れのリスクがない。

```typescript
export const config = {
  matcher: [
    // /auth・/ui・/（トップ）・静的ファイル以外は全部認証必須
    '/((?!auth|ui|api/auth|_next/static|_next/image|favicon.ico|$).*)',
  ],
}
```

| | 許可リスト | 除外リスト |
|--|-----------|-----------|
| 新しいルート追加時 | matcher も更新が必要 | 何もしなくていい |
| 認証し忘れのリスク | ある | ない |
| 読みやすさ | 分かりやすい | 慣れが必要 |

> このプロジェクトでは現在**許可リスト**を使用。ページが増えてきたら除外リストに切り替える。

---

### 9. ナビゲーション関連


App Router では `next/navigation` を使う（Pages Router の `next/router` とは別物）。

```typescript
import { useRouter } from 'next/navigation'  // ✅ App Router
import { useRouter } from 'next/router'       // ❌ Pages Router 用（App Routerでは使えない）

const router = useRouter()
router.push('/todos')    // ページ遷移
router.replace('/todos') // 履歴を残さず遷移
router.refresh()         // Server Component のデータキャッシュを再取得
router.back()            // 戻る

// サーバーサイドでのリダイレクト（Server Component / API Route で使用）
import { redirect, notFound } from 'next/navigation'
redirect('/login')   // 別ページへリダイレクト
notFound()           // 404ページを表示
```

---

### 10. fetch のキャッシュ制御（Next.js 拡張）

Next.js は標準の `fetch` を拡張してキャッシュ制御オプションを追加している。

```typescript
// 毎回最新データを取得（キャッシュなし）← このプロジェクトで使用
fetch(url, { cache: 'no-store' })

// ビルド時にキャッシュ、以降は使い回す（静的生成と同等）
fetch(url, { cache: 'force-cache' })

// 指定秒数後に再取得（ISR: Incremental Static Regeneration）
fetch(url, { next: { revalidate: 60 } })  // 60秒ごとに更新
```

---

## レイヤー別設計詳細

### 1. ページレイヤー（Pages）

**役割**：画面の構成とレイアウト

**学習ポイント**：
- Next.js App Router によるファイルベースルーティング
- サーバーコンポーネント vs クライアントコンポーネント
- SSR（サーバーサイドレンダリング）の理解
- ページのメタデータ管理

**例**：
```typescript
// app/(dashboard)/todos/page.tsx
export default function TodosPage() {
  // ページレベルのロジック
  return <TodoListContainer />
}
```

### 2. コンポーネントレイヤー（Components）

**役割**：再利用可能なUI部品の実装

**分類**：
- **UI Components**：汎用的なボタン、入力欄など（`components/ui/`）
- **Feature Components**：機能別のコンポーネント（`components/auth/`, `components/todos/`）
- **Layout Components**：レイアウト構成用（`components/common/`）

**学習ポイント**：
- React 関数型コンポーネント
- Props による親子通信
- 状態のリフティング（State Lifting）
- コンポーネント設計パターン

**例**：
```typescript
// components/todos/TodoItem.tsx
interface Props {
  todo: Todo
  onDelete: (id: string) => void
  onEdit: (id: string) => void
}

export const TodoItem: React.FC<Props> = ({ todo, onDelete, onEdit }) => {
  return (
    <div className="todo-item">
      <h3>{todo.title}</h3>
      <button onClick={() => onEdit(todo.id)}>編集</button>
      <button onClick={() => onDelete(todo.id)}>削除</button>
    </div>
  )
}
```

### 3. カスタムフックレイヤー（Hooks）

**役割**：ロジックの再利用と状態管理

**主要フック**：
- `useAuth()` ：認証状態とログイン/ログアウト処理
- `useTodos()` ：TODO一覧取得、作成、編集、削除
- `useFetch()` ：汎用API呼び出しロジック
- `useLocalStorage()` ：ブラウザストレージ操作

**学習ポイント**：
- `useState` による状態管理
- `useEffect` による副作用処理
- 依存配列の理解
- カスタムフック作成パターン
- 非同期処理（async/await）

**例**：
```typescript
// hooks/useTodos.ts
export const useTodos = () => {
  const [todos, setTodos] = useState<Todo[]>([])
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState<string | null>(null)

  // TODO取得
  const fetchTodos = useCallback(async () => {
    setLoading(true)
    try {
      const data = await todoService.getTodos()
      setTodos(data)
    } catch (err) {
      setError(err instanceof Error ? err.message : 'エラーが発生しました')
    } finally {
      setLoading(false)
    }
  }, [])

  // TODO作成
  const createTodo = useCallback(async (title: string) => {
    // 実装
  }, [])

  return { todos, loading, error, fetchTodos, createTodo }
}
```

### 4. Contextレイヤー（Context API）

**役割**：アプリケーション全体の共有状態管理

**グローバル状態**：
- ユーザー認証情報（ユーザー、トークン、ログイン状態）
- TODO一覧（全ページで参照する必要がある場合）

**学習ポイント**：
- `useContext` による状態共有
- Context Provider の設計
- 不要な再レンダリングの最適化

**例**：
```typescript
// context/AuthContext.tsx
interface AuthContextType {
  user: User | null
  isLoggedIn: boolean
  login: (email: string, password: string) => Promise<void>
  logout: () => void
}

export const AuthContext = createContext<AuthContextType | undefined>(undefined)

export const AuthProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  const [user, setUser] = useState<User | null>(null)

  const value = {
    user,
    isLoggedIn: !!user,
    login: async (email, password) => {
      // ログイン処理
    },
    logout: () => {
      // ログアウト処理
    }
  }

  return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>
}
```

### 5. Serviceレイヤー（API層）

**役割**：API呼び出しとビジネスロジック、モックデータ

**内容**：
- API通信のロジック（fetch/axios）
- エラーハンドリング
- レスポンスの変換
- モックデータの提供

**学習ポイント**：
- Promise と async/await
- HTTP通信（GET, POST, PUT, DELETE）
- エラーハンドリング
- モックサーバーの実装

**例**：
```typescript
// services/todoService.ts
export const todoService = {
  // TODO一覧を取得
  getTodos: async (): Promise<Todo[]> => {
    const response = await fetch('/api/todos')
    if (!response.ok) throw new Error('取得に失敗しました')
    return response.json()
  },

  // TODOを作成
  createTodo: async (title: string): Promise<Todo> => {
    const response = await fetch('/api/todos', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ title })
    })
    if (!response.ok) throw new Error('作成に失敗しました')
    return response.json()
  },

  // TODO更新
  updateTodo: async (id: string, title: string): Promise<Todo> => {
    // 実装
  },

  // TODO削除
  deleteTodo: async (id: string): Promise<void> => {
    // 実装
  }
}
```

### 6. Typeレイヤー（型定義）

**役割**：TypeScript型の一元管理

**内容**：
- Entity型（User, Todo など）
- API Response/Request型
- UI Props型

**学習ポイント**：
- Interface と Type の使い分け
- ジェネリック型
- Union型、Intersection型

**例**：
```typescript
// types/todo.types.ts
export interface Todo {
  id: string
  title: string
  description: string
  completed: boolean
  createdAt: Date
  updatedAt: Date
}

export interface CreateTodoRequest {
  title: string
  description: string
}

export interface UpdateTodoRequest {
  title?: string
  description?: string
  completed?: boolean
}
```

### 7. API Routesレイヤー（Mock Backend）

**役割**：バックエンド API のシミュレーション

**エンドポイント**：
```
POST   /api/auth/login          → ユーザーログイン
GET    /api/todos               → TODO一覧取得
POST   /api/todos               → TODO作成
GET    /api/todos/[id]          → TODO詳細取得
PUT    /api/todos/[id]          → TODO更新
DELETE /api/todos/[id]          → TODO削除
```

**学習ポイント**：
- Next.js API Routes の基礎
- HTTP ステータスコード
- JSON レスポンス形式
- モックデータの管理

**例**：
```typescript
// app/api/todos/route.ts
export async function GET(request: Request) {
  // モックデータを返す
  return Response.json(mockTodos)
}

export async function POST(request: Request) {
  const body = await request.json()
  const newTodo = createTodo(body)
  return Response.json(newTodo, { status: 201 })
}
```

---

## MSW（Mock Service Worker）

### 概要

API Route を**ネットワークレベル**で横取りするモックライブラリ。
ブラウザの Service Worker が fetch リクエストを捕まえ、`handlers.ts` に書いたレスポンスを返す。

### ファイル構成

```
src/mocks/
  handlers.ts          ← モック定義を一元管理（全エンドポイントをここに書く）
  browser.ts           ← ブラウザ用 Service Worker セットアップ（開発環境）
  server.ts            ← Node.js 用サーバーセットアップ（テスト環境）

public/
  mockServiceWorker.js ← MSW が自動生成。ブラウザで Service Worker として動く
```

### リクエストの処理優先順位

```
Client Component の fetch('/api/todos')
    │
    ▼
【開発環境・MSW 起動中】
Service Worker が横取り → handlers.ts のハンドラが応答
    → API Route は呼ばれない

【本番環境 / handlers.ts に定義なし】
onUnhandledRequest: 'bypass' でスルー → API Route が応答
```

handlers.ts に書いてあるエンドポイント → MSW が処理  
書いていないエンドポイント（`/api/auth/...` など） → API Route がそのまま処理

### handlers.ts の書き方

```typescript
import { http, HttpResponse } from 'msw'

export const handlers = [
  // GET /api/todos → mockTodos をそのまま返す
  http.get('/api/todos', () => {
    return HttpResponse.json(mockTodos)
  }),

  // POST /api/todos → body を受け取って配列に追加
  http.post('/api/todos', async ({ request }) => {
    const body = await request.json()
    mockTodos.push({ id: Date.now().toString(), ...body })
    return HttpResponse.json(body, { status: 201 })
  }),

  // パスパラメータは params で受け取る
  http.put('/api/todos/:id', async ({ params, request }) => {
    const body = await request.json()
    // params.id で /api/todos/123 の "123" が取れる
  }),
]
```

### 環境別の動作まとめ

| 環境 | 使うファイル | 動作 |
|------|------------|------|
| 開発（ブラウザ） | `browser.ts` + `handlers.ts` | Service Worker が fetch を横取り |
| テスト（Vitest） | `server.ts` + `handlers.ts` | Node.js インターセプターが fetch を横取り |
| 本番 | なし | API Route がそのまま応答 |

### MSWProvider

`app/layout.tsx` に組み込んだ Client Component。開発環境でのみ Service Worker を起動する。

```typescript
// 開発環境でのみ worker.start() を呼ぶ
useEffect(() => {
  if (process.env.NODE_ENV !== 'development') return
  import('@/mocks/browser').then(({ worker }) => {
    worker.start({ onUnhandledRequest: 'bypass' })
  })
}, [])
```

### API Route との関係

API Route ファイルは削除しなくてよい。  
- **開発中**：MSW が横取りするため API Route は実質呼ばれない  
- **本番**：MSW が動かないため API Route → 本物の DB、という流れでそのまま使える  
- **Auth.js の `/api/auth/...`**：handlers.ts に定義していないため常に API Route が処理する

---

## フロントエンド処理フロー

### 参照ファイルマップ

```
【サーバーサイド参照】
todos/page.tsx
  └── components/todos/list/TodoListFetcher.tsx  ← データ取得はここに移動
        └── services/todo/shared/todoRepository.ts
              └── services/todo/shared/mockData.ts

[id]/edit/page.tsx                               ← params から id を受け取るだけ
  └── components/todos/edit/TodoEditFetcher.tsx  ← データ取得はここに移動
        └── services/todo/shared/todoRepository.ts
              └── services/todo/shared/mockData.ts

app/api/todos/route.ts                         ← API Route（サーバーで動く）
  └── services/todo/shared/mockData.ts         ← 同じ配列を共有

app/api/todos/[id]/route.ts
  └── services/todo/shared/mockData.ts

【クライアントサイド参照】
components/todos/list/TodoList.tsx
  └── hooks/todo/list/useTodos.ts
        ├── services/todo/list/todoService.ts  ← create・delete
        └── services/todo/edit/todoService.ts  ← update（完了トグル）

components/todos/edit/TodoEditForm.tsx
  └── services/todo/edit/todoService.ts        ← update（編集フォーム保存）
```

---

## データフロー図

### ページ初期表示フロー（SSR）

```
ブラウザが /todos にアクセス
    │
    ▼ HTTPリクエスト
┌─────────────────────────────────────────────┐
│  Node.js ランタイム                          │
│                                             │
│  todos/page.tsx（URLの窓口）                 │
│    └── <TodoListFetcher />                  │
│                                             │
│  TodoListFetcher.tsx（Server Component）     │
│    ├── todoRepository.getAll()              │
│    │     └── mockData.ts ← メモリ上の配列  │
│    │           └── Todo[] 返却             │
│    └── <TodoList initialTodos={todos} />   │
│          ↓ HTML生成（初期データ埋め込み）   │
└─────────────────────────────────────────────┘
    │
    ▼ HTML返却（データ込み）
┌─────────────────────────────────────────────┐
│  ブラウザ                                    │
│    ↓ 表示（初期データがすでに見える）        │
│    ↓ React Hydration                        │
│  TodoList.tsx（Client Component）           │
│    → useState(initialTodos) でインタラクティブ化│
└─────────────────────────────────────────────┘
```

### TODO作成フロー（CSR）

```
ユーザーが「新規作成」ボタンをクリック
    │
    ▼ [ブラウザ / Client Component]
TodoList.tsx
    │ showForm = true → TodoForm表示
    ▼
TodoForm.tsx（React Hook Form + Zod）
    │ 入力・バリデーション
    │ handleSubmit → onSubmit(data)
    ▼
useTodos.ts → createTodo(data)
    │
    └── todoService.ts → fetch('POST /api/todos', data)
              │
              ▼ HTTPリクエスト
    ┌─────────────────────────────────┐
    │  Node.js ランタイム              │
    │  app/api/todos/route.ts         │
    │    └── mockData.push(newTodo)   │
    │    └── return 201 + newTodo     │
    └─────────────────────────────────┘
              │
              ▼ レスポンス（新しいTodo）
useTodos.ts → setTodos([...todos, newTodo])
    │
    ▼ 再レンダリング
TodoList.tsx → 新しい項目が一覧に表示される
```

### TODO編集フロー（SSR → CSR）

```
ユーザーが「編集」ボタンをクリック
    │
    ▼ [ブラウザ]
Link → /todos/[id]/edit に遷移
    │
    ▼ HTTPリクエスト
┌─────────────────────────────────────────────┐
│  Node.js ランタイム                          │
│                                             │
│  [id]/edit/page.tsx（Server Component）      │
│    │                                        │
│    ├── todoRepository.getById(id)            │
│    │     └── mockData.ts ← 該当Todoを返す   │
│    │                                        │
│    └── <TodoEditForm todo={todo} />         │
│          ↓ HTML生成（既存データ埋め込み）    │
└─────────────────────────────────────────────┘
    │
    ▼ HTML返却
TodoEditForm.tsx（Client Component）
    │ TodoForm に defaultValues として渡す
    │ ユーザーが編集・送信
    └── todoService.update(id, data)
              │
              ▼ fetch('PUT /api/todos/[id]')
    ┌─────────────────────────────────┐
    │  app/api/todos/[id]/route.ts    │
    │    └── mockData[index] 更新     │
    └─────────────────────────────────┘
              │
              ▼
router.push('/todos') + router.refresh()
    → /todos に戻り、一覧を再取得
```

### TODO削除フロー（CSR）

```
ユーザーが「削除」ボタンをクリック
    │
    ▼
window.confirm → OK
    │
    ▼ [ブラウザ / Client Component]
useTodos.ts → deleteTodo(id)
    │
    └── todoService.delete(id)
              │
              ▼ fetch('DELETE /api/todos/[id]')
    ┌─────────────────────────────────┐
    │  app/api/todos/[id]/route.ts    │
    │    └── mockData.splice(index,1) │
    │    └── return 204 No Content    │
    └─────────────────────────────────┘
              │
              ▼
useTodos.ts → setTodos(todos.filter(t => t.id !== id))
    │
    ▼ 再レンダリング
TodoList.tsx → 削除した項目が消える
```

### 認証フロー（ログイン）

```
ユーザーが /auth/login を開く
    │
    ▼ [ブラウザ / Client Component]
LoginForm.tsx（RHF + Zod）
    │ フォーム入力・バリデーション
    │ handleSubmit → loginAction(email, password)
    ▼
[Server Action: app/(auth)/login/actions.ts]
    │ signIn('credentials', { email, password, redirectTo: '/todos' })
    ↓ POST /api/auth/[...nextauth]
┌───────────────────────────────────────────┐
│  Auth.js ハンドラ                          │
│    → authorize() で email/password 検証   │
│    → 一致 → JWT 生成                      │
│    → HttpOnly Cookie に保存               │  ← XSS対策：JSからアクセス不可
└───────────────────────────────────────────┘
    │
    ▼ NEXT_REDIRECT（内部的な例外）→ /todos へリダイレクト
ブラウザが /todos を開く
    │
    ▼ proxy.ts が実行
req.auth が存在 → 通過
    │
    ▼ (dashboard)/layout.tsx
Header.tsx（Server Component） → await auth() でセッション取得 → メール表示
LogoutButton.tsx（Client Component） → signOut() でログアウト
```

**ポイント：Server Action でのリダイレクト処理**
```typescript
// app/(auth)/login/actions.ts
try {
  await signIn('credentials', { email, password, redirectTo: '/todos' })
} catch (error) {
  if (error instanceof AuthError) {
    return { error: '認証情報が正しくありません' }
  }
  throw error  // ← NEXT_REDIRECT は必ず再スローする（これがリダイレクトを実行する）
}
```

### ページレンダリングフロー（SSR）

```
ユーザーがURLアクセス
    ↓
[Next.js Server]
    ↓ (page.tsx実行)
[サーバーコンポーネント]
    ↓ (データ取得)
[データベース/API]
    ↓ (HTML生成)
[Server → Client]
    ↓
[ブラウザで表示]
    ↓ (Hydration)
[クライアント側でインタラクティブ化]
```

---

## 技術選定理由と学習価値

| 技術 | 選定理由 | 学習価値 |
|------|---------|--------|
| **Next.js 13+** | モダンなフルスタックフレームワーク | ファイルベースルーティング、SSR、API Routes の理解 |
| **TypeScript** | 型安全性による品質向上 | 型システム、インターフェース設計、大規模開発への準備 |
| **React Hooks** | 最新の React 開発パターン | 状態管理、副作用処理、カスタムフック作成 |
| **Context API** | ビルトイン状態管理 | Redux より学習負荷が低い、小〜中規模アプリに最適 |
| **React Hook Form** | フォーム状態管理 | 非制御コンポーネントによる高パフォーマンス、実務標準 |
| **Zod** | スキーマバリデーション | 型安全なバリデーション、React Hook Formと組み合わせて使用 |
| **Auth.js（NextAuth）** | 認証管理 | HttpOnly CookieによるJWT管理、セキュアな実装 |
| **Vitest + Testing Library** | 単体・統合テスト | Vite互換の高速テストランナー |
| **Playwright** | E2Eテスト | ブラウザ操作を自動化した結合テスト |
| **Husky + lint-staged** | コード品質管理 | コミット前にESLint/Prettierを自動実行 |
| **Tailwind CSS** | ユーティリティ CSS フレームワーク | デザイン効率化、レスポンシブ設計（オプション） |
| **API Routes** | バックエンド学習なし | モックサーバー実装、HTTP通信の理解 |

---

## 開発フェーズ

### フェーズ 1：基礎セットアップ
- [ ] プロジェクト初期化（`create-next-app`）
- [ ] ディレクトリ構造作成
- [ ] TypeScript設定
- [ ] 基本レイアウト実装
- [ ] Husky + lint-staged + ESLint + Prettier 設定
- [ ] Vitest + Testing Library 設定
- [ ] Playwright 設定
- [ ] ErrorBoundary コンポーネント実装

### フェーズ 2：認証機能実装
- [x] Auth.js v5（next-auth@beta）導入・設定（JWT / HttpOnly Cookie）
- [x] Credentials provider + モック認証（test@example.com / password123）
- [x] `src/auth.ts` ― NextAuth 設定・`handlers` / `signIn` / `signOut` / `auth` をエクスポート
- [x] `app/api/auth/[...nextauth]/route.ts` ― Auth.js ハンドラ（GET/POST）
- [x] `proxy.ts` ― `/todos/:path*` を認証必須に設定
- [x] `types/auth/login/auth.schema.ts` ― ログインフォームの Zod スキーマ
- [x] `app/(auth)/login/actions.ts` ― `signIn` を呼ぶ Server Action
- [x] `components/auth/login/LoginForm.tsx` ― RHF + Zod + Server Action
- [x] `app/(auth)/login/page.tsx` ― ログインページ
- [x] `components/common/Header.tsx` ― ログイン中ユーザー表示（Server Component）
- [x] `components/common/LogoutButton.tsx` ― ログアウトボタン（Client Component）
- [x] `app/(dashboard)/layout.tsx` ― ダッシュボードレイアウト（Header 込み）
- [ ] AuthContext 構築（次フェーズで必要になれば追加）

### フェーズ 3：TODO CRUD実装
- [ ] Todo型定義・Zodスキーマ定義
- [ ] TodoForm コンポーネント（React Hook Form + Zod）
- [ ] TodoList コンポーネント
- [ ] useTodos カスタムフック
- [ ] todoService 実装
- [ ] API Routes 実装（GET, POST, PUT, DELETE）

### フェーズ 4：ポーランド（改善）
- [ ] UI/UX改善
- [ ] エラーハンドリング強化
- [ ] ローディング表示
- [ ] バリデーション拡張
- [ ] テスト追加（オプション）

### フェーズ 5：ライブラリ移行（後対応）
- [x] **MSW導入**：純粋関数モック → ネットワークレベルモックへ切り替え
- [ ] **TanStack Query導入**：useState による手動ローディング/エラー管理を置き換え
- [ ] バックエンド（Go/gin）との結合

---

## コーディングルール

### 1. コメント規則（初心者向け）

```typescript
// React 関数型コンポーネント：この構文は React 18 以降の標準形式です
export const MyComponent: React.FC<Props> = ({ prop1, prop2 }) => {
  // useState の構文：状態 = [現在の値, 値を更新する関数] = useState(初期値)
  const [count, setCount] = useState(0)

  // useEffect の構文：副作用（データ取得など）を定義します
  // 第2引数の依存配列 [] は、このコンポーネントがマウントされた時のみ実行
  useEffect(() => {
    // ここにデータ取得などの処理を書きます
    fetchData()
  }, [])

  return <div>...</div>
}
```

### 2. ネーミング規則

- **コンポーネント**：PascalCase（例：`TodoItem`, `LoginForm`）
- **関数/変数**：camelCase（例：`getTodos`, `isLoading`）
- **定数**：UPPER_SNAKE_CASE（例：`MAX_TITLE_LENGTH`, `API_BASE_URL`）
- **型/Interface**：PascalCase（例：`Todo`, `User`）
- **ファイル**：
  - コンポーネント：PascalCase（`TodoItem.tsx`）
  - その他：camelCase（`todoService.ts`, `useAuth.ts`）

### 3. 型定義ルール

```typescript
// すべてのコンポーネント Props に型を付与
interface TodoItemProps {
  todo: Todo
  onDelete: (id: string) => void
}

// API レスポンスに型を付与
interface ApiResponse<T> {
  data: T
  error: string | null
  status: number
}

// 関数の引数と戻り値に型を付与
const getTodos = async (userId: string): Promise<Todo[]> => {
  // 実装
}
```

### 4. TODO機能の CRUD 実装

```typescript
// Create：新規作成
const createTodo = async (title: string): Promise<Todo> => {}

// Read：取得
const getTodos = async (): Promise<Todo[]> => {}
const getTodoById = async (id: string): Promise<Todo> => {}

// Update：更新
const updateTodo = async (id: string, updates: Partial<Todo>): Promise<Todo> => {}

// Delete：削除
const deleteTodo = async (id: string): Promise<void> => {}
```

---

## 参考資料・学習ステップ

### 推奨学習順序

1. **React 基礎**
   - コンポーネント、Props、State
   - hooks（useState, useEffect, useContext）

2. **Next.js 基礎**
   - App Router、ファイルベースルーティング
   - Server Components vs Client Components

3. **TypeScript**
   - 基本型、Interface、Generic
   - 関数の型付け

4. **状態管理**
   - Context API
   - カスタムフック

5. **API通信**
   - fetch、async/await
   - エラーハンドリング

6. **実装実践**
   - 実際のコンポーネント実装
   - CRUD機能の実装

---

## 注意事項

- **モックデータ**：現在はAPI Routesにモックデータを返すようにします。将来的にバックエンドを追加する場合は、serviceレイヤーの実装を変更するだけで対応可能です。

- **SSR vs CSR**：このプロジェクトではSSRを活用しますが、個別のコンポーネントはクライアント側で インタラクティブに動作します。

- **スタイリング**：Tailwind CSSの使用はオプションです。必要に応じてCSS Modulesなどの他のソリューションも検討してください。

---

## まとめ

このアーキテクチャは、フロントエンド初心者が段階的に学習できるように設計されています。

- **層の分離**により、各部分の責任が明確
- **コンポーネント指向**により、再利用性と保守性が高い
- **モックAPI**により、バックエンド開発なしに学習可能
- **丁寧なコメント**により、初心者も理解しやすい

次のステップ：**プロジェクト初期セットアップ**
